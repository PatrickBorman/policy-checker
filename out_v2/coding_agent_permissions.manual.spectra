module coding_agent_permissions

env boolean req_shell;
env boolean req_write;
env boolean in_sandbox;
env boolean user_approved;
sys boolean allow;
sys boolean deny;
sys boolean ask;

// G1
guarantee
	G ((req_shell | req_write) -> (allow | deny | ask));
// G1
guarantee
	G (!(allow & deny) & !(allow & ask) & !(deny & ask));
// G1
guarantee
	G (!(req_shell | req_write) -> (!allow & !deny & !ask));
// G2
guarantee
	G ((req_shell & !in_sandbox & !user_approved) -> !allow);
// G3
guarantee
	G ((req_write & in_sandbox & !req_shell) -> allow);
// G4
guarantee
	G (((req_shell | req_write) & user_approved) -> allow);
// A1
assumption
	G (user_approved -> (req_shell | req_write));
